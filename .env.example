# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=infrastructure_training
DB_SSLMODE=disable

# Database admin credentials (for database initialization)
DB_ADMIN_USER=postgres
DB_ADMIN_PASSWORD=postgres

# Server configuration
PORT=8080